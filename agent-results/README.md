# Agent results

One evidence file per task: `NNN-result.md`.

Include task ID, status (`PASS`, `BLOCKED`, `CHANGES_REQUIRED`), files changed, exact commands/results, runtime evidence where required, assumptions, blockers/risks, and next recommendation.
