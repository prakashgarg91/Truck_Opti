# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['app.py'],
    pathex=['D:\\Github\\Truck_Opti\\TruckOptimum'],
    binaries=[],
    datas=[
        ('templates', 'templates'),
        ('static', 'static'),
        ('tests', 'tests'),
        ('pytest.ini', '.'),
        ('requirements_test.txt', '.'),
        ('run_tests.py', '.'),
        ('test_runner.bat', '.'),
        ('test_runner.sh', '.'),
        ('TEST_DOCUMENTATION.md', '.'),
        ('TESTING_QUICK_REFERENCE.md', '.'),
        ('error_logger.py', '.'),
    ],
    hiddenimports=[
        'flask',
        'sqlite3',
        'json',
        'os',
        'sys',
        'tempfile',
        'threading',
        'time',
        'logging',
        'datetime',
        'subprocess',
        'pathlib',
        'pytest',
        'requests',
        'coverage',
        'psutil',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='TruckOptimum_v2.4.0_Complete_Testing_Framework',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=None,
)